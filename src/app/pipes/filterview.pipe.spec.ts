import { FilterviewPipe } from './filterview.pipe';

describe('FilterviewPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterviewPipe();
    expect(pipe).toBeTruthy();
  });
});
